class: "EvalTestMultiplePossibilities"
success: "PASS"
failure: "NO PASS"

# A python expression to be evaluated.  This expression must return the 
# same result for the student and instructor's code.

test: "logicPlan.model_to_string(logicPlan.find_model(logicPlan.sentence3()))"

